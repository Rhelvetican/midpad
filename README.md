# Midpad

Simple command line utility to pad texts.

```
Usage: midpad.exe [OPTIONS]

Options:
    -r, --raw               <RAW>
    -f, --file              <FILE>
    -o, --output            <OUTPUT>
    -h, --help              Print help
    -V, --version           Print version
```
